@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
<ul class="navbar-nav">
    <li class="nav-item">
        @using (Html.BeginForm("Index", "Home", FormMethod.Get))
        {
            <div class="row" style="margin-top: 5px; margin-left: 10px;">
                <div class="col-xs-6" style="width: 400px">
                        @Html.Editor("timkiem",new { htmlAttributes = new {@class = "form-control",autocomplete = "off", placeholder = "Tìm tên tác phẩm, tác giả, nhà xuất bản bạn mong muốn...", style = "border-radis:10px;font-size:14px" } })
                </div>
                <div class="col-xs-6">
                    <button id="searchbutton" type="submit" class="btn btn-secondary form-control" value="" style="width: 105px;text-align:left;font-size:14px" onclick="myFunction()">
                        <i class="fas fa-search"></i> Tìm kiếm
                    </button>
                </div>
            </div>
        }
    </li>
</ul>
